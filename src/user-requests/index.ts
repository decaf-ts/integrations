/**
 * @module integrations/user-requests
 * @summary User Request Resolution Engine surface exported to integrations
 * consumers.
 * @description Re-exports the `@decaf-ts/ui-decorators/user-requests` barrel so
 * integrations packages can hand user-request handling to app consumers
 * through a flat entry point.
 */
export * from '@decaf-ts/ui-decorators/user-requests';
