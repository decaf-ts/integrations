/**
 * @module integrations/graph/decorators
 * @summary Reserved for future core-level graph decorators.
 * @description This file is intentionally empty for now. Graph decorators such as `@pinnable()` live in `@decaf-ts/ui-decorators/graph`. Additional decorators may be added here if the core engine needs its own metadata keys.
 */
export {};

